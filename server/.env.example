# Example DATABASE_URL for PostgreSQL (replace with your DB credentials)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/drivemerge"

# Export this as DATABASE_URL in your shell before running `yarn prisma migrate dev`

DM_JWT_SECRET=drivemerge-dev-secret
